id: com.avocode.Avocode
sdk: org.freedesktop.Sdk
runtime: org.freedesktop.Platform
runtime-version: '19.08'
base: org.electronjs.Electron2.BaseApp
base-version: '19.08'
finish-args:
  - --share=ipc
  - --socket=x11
  - --socket=wayland
  - --device=dri
  - --share=network
  - --system-talk-name=org.bluez
  - --filesystem=home
command: avocode
cleanup:
  - '*.a'
  - '*.la'
  - /include
  - /lib/pkgconfig
  - /share/man
modules:

  - name: avocode
    buildsystem: simple
    build-commands:
      - install -Dm755 avocode.sh /app/bin/avocode
      - install -Dm755 apply_extra.sh /app/bin/apply_extra
      - install -Dm755 /usr/bin/desktop-file-edit -t /app/bin/
      - install -Dm755 /usr/bin/ar -t /app/bin/
      - install -Dm755 /usr/lib/$(gcc -print-multiarch)/libbfd-*.so -t /app/lib/
      - install -Dm644 ${FLATPAK_ID}.appdata.xml -t /app/share/appdata/
    sources:
      - type: extra-data
        filename: avocode.deb
        only-arches:
          - x86_64
        url: https://linux.avocode.com/apt/pool/main/a/avocode/avocode_4.6.0-8403_amd64.deb
        sha256: 586b84deb1b37d659c7de1f1d64530621f585f930feb86235e3df670801e9ff3
        size: 69893060
        x-checker-data:
          type: debian-repo
          package-name: avocode
          root: https://linux.avocode.com/apt
          dist: all
          component: main

      - type: script
        dest-filename: avocode.sh
        commands:
          - export TMPDIR="$XDG_RUNTIME_DIR/app/$FLATPAK_ID"
          - exec zypak-wrapper /app/extra/avocode/avocode "$@"

      - type: file
        path: apply_extra.sh

      - type: file
        path: com.avocode.Avocode.appdata.xml
